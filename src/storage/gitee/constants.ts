/** Final object size limit for Gitee Contents API uploads. */
export const MAX_UPLOAD_BYTES = 2 * 1024 * 1024

/** Reject absurd originals before sharp burns memory. */
export const MAX_INPUT_BYTES = 40 * 1024 * 1024
